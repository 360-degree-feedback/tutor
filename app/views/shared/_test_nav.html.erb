<div id="lesson-nav">
  <ul class="nav nav-pills nav-stacked">
    <% @lessons.each do |lesson| %>
        <li class="<%= 'active' if lesson.id == @lesson.id %>"><%= link_to lesson.title, lesson %></li>
    <% end %>
  </ul>
</div>