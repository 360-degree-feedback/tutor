<div id="lesson-nav" class="col-md-2">
  <ul class="nav nav-pills nav-stacked">
    <% @lessons.each do |lesson| %>
        <li class="<%= 'active' if lesson.id == params[:id].to_i %>"><%= link_to lesson.title, lesson %></li>
    <% end %>
  </ul>
</div>